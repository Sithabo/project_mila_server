# telemetry/

ROS2 GNSS/IMU subscriber code and the future JSON/WebSocket telemetry adapter will
live here.

The validated ROS2 sensor interface (topics, message types, rates, and which fields
to use) is documented in `docs/ROS2_SENSOR_INTERFACE.md`. No project code has been
written yet — this package is currently scaffolding only.
