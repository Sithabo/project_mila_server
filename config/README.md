# config/

Runtime configuration for the visualization stack (camera role/topology mapping,
encoder settings, transport endpoints, etc.) will live here.

No configuration files exist yet. `config/local*` is gitignored for any
machine-specific overrides added later.
